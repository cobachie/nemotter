<!DOCTYPE html>

<head>
</head>


<body>
<p id="notice"><%= notice %></p>



<div class="pagetop_form">
  <div class="tag_form"><!--タグ検索フォーム-->
<!--
<div class="col-lg-6">
    <div class="input-group">
      <input type="text" class="form-control" placeholder="タグ検索">
      <span class="input-group-btn">
        <button class="btn btn-default" type="button">Go!</button>
      </span>
    </div> --> <!-- /input-group -->
  <!--</div>--><!-- /.col-lg-6 -->
</div>
<div class="create_form"> <!--メッセージ作成フォーム-->
<div class="actions"><a href="messages/new.html"class="btn btn-primary btn-lg active" role="button">
メッセージ作成
</a></div></div>
<div class="date_form"><!--日時入力フォーム-->
<!--<%= datetime_select :page, :updated_at , :discard_minute=>true, :discard_hour=>true%>
</div>-->
</div>

  <% @messages.reverse_each do |message| %>
  <div class="article">
  <div class="detail">
    タグ:<td><%= message.tag %></td>
  </div>
  <div class="comment"><p>
    <%= message.body %>
  </p></div>
  <div class="posted_date">
    date:
  </div>
  </div>
  <!--<%= link_to 'Edit', edit_message_path(message) %> -->
<% end %>

<!-- <%= link_to 'New Message', new_team_message_path(team_id: @team.id) %> -->

<div class="top_button">
<div class="actions"><a href="messages"class="btn btn-primary btn-lg active" role="button">
TOP
</a></div>
</div>

</body>
<!--
<table>
  <thead>
    <tr>
      <th>Body</th>
      <th>Tag</th>
      <th>User</th>
      <th>Team</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td><%= message.body %></td>
        <td><%= message.tag %></td>
        <td><%= message.user_id %></td>
        <td><%= message.team_id %></td>
        <td><%= link_to 'Show', message %></td>
        <td><%= link_to 'Edit', edit_message_path(message) %></td>
        <td><%= link_to 'Destroy', message, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
-->
